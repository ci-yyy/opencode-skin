{
  "name": "opencode-skin",
  "version": "0.1.0",
  "description": "OpenCode 桌面客户端换肤：收割当前主题变量 → 按色相配方整体染色 → CDP 注入（零依赖，Node 22+）",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test"
  }
}
