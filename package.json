{
  "name": "opencode-skin",
  "version": "0.1.0",
  "description": "macOS 单机 OpenCode 换肤工具：CSS 变量注入 + 皮肤守护进程 + 图片生成主题（零依赖，Node 22+）",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test"
  }
}
