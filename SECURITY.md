# 安全说明（SECURITY）

## 威胁模型

本工具的攻击面主要来自一个本地端口：

- **9345（OpenCode 调试端口）**：由 OpenCode 自身提供（`apply-skin.sh` 带参数启动时开启），
  本工具只是使用方。CDP 协议**没有身份认证**。

该端口只绑定 `127.0.0.1` 本机回环，不对外网开放。但同一台电脑上以**同一用户权限**运行的
任何进程（包括你运行的其他软件、浏览器里被 XSS 的页面通过 localhost 访问等场景）都能连接它：

- 连上 9345 可以读取/控制 OpenCode 渲染进程（执行任意 JS）

本工具自身**不监听任何端口**（没有界面内面板，也就没有数据服务）。

## 缓解措施

- 注入的 CSS 只覆盖 CSS 变量，不执行远程代码、不请求外部网络
- 工具不修改 OpenCode 安装目录、签名、会话数据；随时可整体移除

## 如果你在意这个风险

1. `bash uninstall.sh`（加 `--purge` 连工具目录一起删）
2. 完全退出 OpenCode，从启动台/访达正常打开（调试端口消失，风险窗口关闭）

## 报告安全问题

请通过 GitHub 仓库的私有渠道联系维护者，不要公开提交 Issue。
